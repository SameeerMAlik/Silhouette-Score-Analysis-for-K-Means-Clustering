# Silhouette-Score-Analysis-for-K-Means-Clustering
This project demonstrates how to evaluate the performance of the K-Means clustering algorithm using **Silhouette Analysis**. It generates a synthetic dataset with `make_blobs`, applies K-Means clustering for different values of **K**, computes the **Silhouette Score**, and visualizes the results using Silhouette plots and cluster scatter plots.
